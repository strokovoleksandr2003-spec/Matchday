{
  "name": "matchday-backend",
  "version": "0.3.0",
  "description": "API layer for the Matchday app — Express app, Supabase (Postgres) for storage, deployed to Netlify as a serverless function.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5",
    "cors": "^2.8.5",
    "@supabase/supabase-js": "^2.45.4",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
